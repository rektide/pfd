[package]
name = "context"
version = "0.1.0"
edition = "2024"

[features]
default = []
check-bytes = ["bytecheck"]

[dependencies]
anyhow = "1"
rkyv = { version = "0.8", features = ["bytecheck"] }
bytecheck = { version = "0.8", optional = true }
